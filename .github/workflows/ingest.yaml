name: Weekly Football Data Ingestion

on:
  schedule:
    # Runs every Monday at 02:00 AM UTC
    - cron: '0 2 * * 1'
  workflow_dispatch: # Allows you to manually click a "Run" button in GitHub

jobs:
  ingest:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas

      - name: Run Ingestion Script
        # We tell it to run the script from the src folder
        run: python src/ingest.py

      - name: Commit and Push Changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action Bot"
          git add data/football_data.db
          git commit -m "Auto-update raw match database [skip ci]" || exit 0
          git push